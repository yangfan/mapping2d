## 2D lidar mapping

## todo

1. map to map icp for loop closure alignment, the match ratio therefore could be much higher than 0.4 for scan to map
2. detect degeneral case, using like line fitting. the use different parameters of icp and loop closure in degeneral case
